name: loon
url: https://github.com/andremedeiros/loon
environment:
  LOON: Was here
services:
  postgresql:
  memcached:
  redis:
languages:
  ruby:
tasks:
  generate:
    description: Does this and that
    command: |
      go generate ...
